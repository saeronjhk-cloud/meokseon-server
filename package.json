{
  "name": "meokseon-server",
  "version": "1.0.0",
  "description": "먹선(吃選) 식품 안전 정보 API 서버",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "cross-env NODE_ENV=development node src/server.js",
    "test": "cross-env NODE_ENV=test node tests/test_traffic_light.js && cross-env NODE_ENV=test node tests/test_racc_policy.js && cross-env NODE_ENV=test node tests/test_100_products.js && cross-env NODE_ENV=test node tests/test_ocr_parser.js && cross-env NODE_ENV=test node tests/test_api.js && cross-env NODE_ENV=test node tests/test_firebase_auth.js && cross-env NODE_ENV=test node tests/test_pulse_consent_service.js && cross-env NODE_ENV=test node tests/test_user_routes.js && cross-env NODE_ENV=test node tests/test_scan_routes.js && npm run test:contracts",
    "test:unit": "cross-env NODE_ENV=test node tests/test_traffic_light.js",
    "test:integration": "node tests/test_100_products.js",
    "test:api": "node tests/test_api.js",
    "test:ocr": "node tests/test_ocr_parser.js && cross-env NODE_ENV=test node tests/test_ocr_nutrition_regression.js && cross-env NODE_ENV=test node tests/test_paren_total_calories.js",
    "test:auth": "cross-env NODE_ENV=test node tests/test_firebase_auth.js",
    "test:pulse-consent": "cross-env NODE_ENV=test node tests/test_pulse_consent_service.js",
    "test:user-routes": "cross-env NODE_ENV=test node tests/test_user_routes.js",
    "test:scan-routes": "cross-env NODE_ENV=test node tests/test_scan_routes.js",
    "test:racc-table": "cross-env NODE_ENV=test node tests/test_racc_table.js",
    "test:serving-resolver": "cross-env NODE_ENV=test node tests/test_serving_resolver.js",
    "test:official-source": "cross-env NODE_ENV=test node tests/test_official_source.js",
    "test:per-total": "cross-env NODE_ENV=test node tests/test_per_total_wiring.js",
    "test:withheld-client": "cross-env NODE_ENV=test node tests/test_withheld_client_render.js",
    "test:paren-total": "cross-env NODE_ENV=test node tests/test_paren_total_calories.js",
    "test:ocr-confirm": "cross-env NODE_ENV=test node tests/test_ocr_confirm.js",
    "_note:test:ocr-confirm": "★ 세션64 — 제품명 확정 축. `analysis_token`(TTL 10분 메모리 캐시) · `POST /api/ocr/confirm`(400 빈 이름 / 410 만료) · `mergeProductMeta`(세션44 치명B 와 같은 함정: 빈 객체도 truthy) · `crowdsourceService` 게이트 0(첫 원재료명 폴백 폐기). 근거: 실물 라벨 67건 실측 — OCR 이 뽑은 값 중 제품명으로 쓸 수 있는 것 33/67(49.3%), 21건이 폴백을 타 '정제수'(6건)·'주정'·'륨' 이 products.product_name 에 들어갈 값이었다. 소스 문자열을 읽지 않는다 — pglite 에 000_baseline.sql 을 적용하고 실제 라우터를 HTTP 로 부른 응답과 DB 에 박힌 행만 단정한다. 뮤테이션 4종(product_meta `||` 폴백 부활 · 첫 원재료명 폴백 부활 · 400 검사 제거 · 클라이언트 분석값으로 서버 값 덮기) 전부 빨강인 것을 확인했다.",
    "test:partial-save": "cross-env NODE_ENV=test node tests/test_nutrition_partial_save.js",
    "_note:test:partial-save": "★ 세션64b — 영양 미확보 «부분 저장» 축. 외부 검토 2명 결론(저장 ≠ 표시 ≠ 검증 · Ingest Permissive, Render Conservative)을 회귀로 고정한다. 지키는 것: ① 영양 실패(기준 판별 실패·이상치·공공데이터 보호)가 원재료·알레르기 저장까지 반려하지 않는다 ② 영양소 0개는 저장 거부가 아니라 nutrition_status='incomplete' ③ 영양 미확보 제품에 신호등·nutrition 블록이 나가지 않는다(빈 값이 0 으로 보일 경로 없음) ④ 알레르기 계약(allergens_available·flat_complete)의 의미가 «안 바뀌었다» — not detected ≠ absent, 부분 저장이 product_allergens 마스터를 쓰지 않는다 ⑤ 영양소 개수는 관측 지표(contributions.data.nutrient_count + 로그)일 뿐 저장을 거부하지 않는다(1~4개 구간에 하한 없음) ⑥ 열량 0 kcal 라벨이 「영양 없음」이 되지 않는다(종전 truthy 검사 함정) ⑦ 기존 바코드 재제보 — 공공데이터 영양은 지키고 원재료·알레르기는 받는다 ⑧ 「확인한 것이 없다면 부분 확인도 아니다」 — 영양 0개는 partial 승격 금지 ⑨ 영양 미확보 기여 3건이 병합돼도 기존 영양값을 NULL 로 지우지 않는다(mergeService UPSERT 에 COALESCE 가 없다 — 세션64b 가 새로 여는 유일한 구멍). 소스 문자열을 읽지 않는다: pglite 에 000_baseline.sql 을 적용하고 정본 서비스를 실제 호출해 DB 에 박힌 행만 단정한다. 뮤테이션 4종(merge 널가드 제거 · partial 승격 조건 되돌리기 · hasNutrition truthy 부활 · basis unknown 전체반려 부활) 전부 빨강인 것을 확인했다.",
    "test:allergen": "cross-env NODE_ENV=test node tests/test_allergen_v2.js && cross-env NODE_ENV=test node tests/test_allergen_declared.js && cross-env NODE_ENV=test node tests/test_allergen_evidence_level.js && cross-env NODE_ENV=test node tests/test_allergen_name_normalize.js && npm run test:parse-allergy",
    "test:allergen-level": "cross-env NODE_ENV=test node tests/test_allergen_evidence_level.js",
    "test:allergen-names": "cross-env NODE_ENV=test node tests/test_allergen_name_normalize.js",
    "test:parse-allergy": "cross-env NODE_ENV=test node tests/test_parse_allergy.js",
    "test:parse-allergy:strict": "cross-env NODE_ENV=test PARSE_ALLERGY_STRICT=1 node tests/test_parse_allergy.js",
    "_note:test:parse-allergy": "★ 세션50 — HACCP 적재 경로(19·26)의 parseAllergy 회귀. 결함 3건(/없음/ 부분일치 · 15자 절단+문장 미분리 · canonical 미정본화)을 고치기 **전에** 대장으로 먼저 고정하고 고쳤다. 측정 기준선: 전사 68건 Tier A 586쌍에서 L1 미검출 99(16.9%) → 0, L2 이름 소실 89~103 → 0, L4 오검출 2 → 2. ⚠ 이 회귀는 `게`(allergenName 소관)나 `밀` 오검출을 owner 표시로 구분해 STRICT 에서 세지 않는다 — 소관 밖 결함을 여기서 고치려 하지 말 것. ⚠ 코드는 고쳐졌지만 **운영 DB 는 재적재 전까지 낡은 채로 남는다**(비정본 705행 + 새 알레르겐 3행 소실). 재적재 순서는 26 → 19 → 76 이며 76 을 먼저 돌리면 두 번 백필해야 한다.",
    "test:merge": "cross-env NODE_ENV=test node tests/mergeService.test.js",
    "test:parity": "cross-env NODE_ENV=test node tests/test_parser_parity.js",
    "test:serving": "npm run test:racc-table && npm run test:serving-resolver && npm run test:official-source && npm run test:per-total && npm run test:withheld-client",
    "probe:basis-unknown": "cross-env NODE_ENV=test node scripts/74-probe-basis-unknown.js",
    "migrate": "psql $DATABASE_URL -v ON_ERROR_STOP=1 -f scripts/migrations/000_baseline.sql && psql $DATABASE_URL -v ON_ERROR_STOP=1 -f scripts/migrations/000b_seed_config.sql",
    "_note:migrate": "★ 세션47 — 체인을 baseline 기반으로 바꿨다. 근거: (1) 001 은 운영을 재현할 수 없다 — users.user_id 가 UUID 인데 운영은 bigint 라 007 의 FK 가 깨진다. (2) 011 이 nutrition_data.serving_size 를 참조하는데 그 컬럼을 만드는 마이그레이션이 없어 011~019 가 연쇄 붕괴한다. (3) products.image_url · additives.e_number 처럼 운영에만 있고 마이그레이션에 없는 컬럼이 다수다. 001~020 은 역사로 남기고 실행하지 않는다. 000_baseline.sql 하나가 운영 덤프(IP/production_schema_2026-07-31.txt) 기준 정본이다. 성립 증명: npm run verify:fresh-schema",
    "_note:migrate2": "★ psql 호출에는 반드시 -v ON_ERROR_STOP=1 을 넣을 것. 없으면 마이그레이션이 실패해도 && 가 이어져 「거짓 초록」이 된다(세션46 §5-4). 새 마이그레이션(021+)은 이 체인 뒤에 같은 형식으로 이어 붙인다.",
    "test:parity-paths": "cross-env NODE_ENV=test node tests/test_path_parity.js",
    "test:parity-paths:strict": "cross-env NODE_ENV=test PARITY_STRICT=1 node tests/test_path_parity.js",
    "test:allergen-contract": "cross-env NODE_ENV=test node tests/test_allergen_contract.js",
    "test:allergen-contract:strict": "cross-env NODE_ENV=test ALLERGEN_STRICT=1 node tests/test_allergen_contract.js",
    "test:schema-constraints": "cross-env NODE_ENV=test node tests/test_schema_constraints.js",
    "test:schema-constraints:strict": "cross-env NODE_ENV=test SCHEMA_STRICT=1 node tests/test_schema_constraints.js",
    "test:contracts": "npm run test:parity-paths && npm run test:allergen-contract && npm run test:schema-constraints",
    "test:contracts:strict": "npm run test:parity-paths:strict && npm run test:allergen-contract:strict && npm run test:schema-constraints:strict",
    "_note:test:contracts": "★★ 세션48 외부검증 1순위 — 「의미를 바꾸기 전에 검증 장치를 먼저 만든다」. 이 세 파일은 소스 문자열을 읽지 않는다. 전부 pglite 에 행을 심고 실제 함수를 호출해 나온 객체만 단정한다. 배경: 4차 검증에서 allergens_flat_complete 를 「항상 true」로 바꿔도, RACC 배선을 통째로 제거해도 12개 테스트 파일이 전부 초록이었다(소스 정규식 검사가 본문 오염으로 뚫렸다). ★ 기본 모드는 「알려진 결함 대장과 일치하는가」만 본다(EXIT 0). 새 불일치 · 대장 값 변화 · **고쳐졌는데 대장에 남아 있는 것** 전부 EXIT 1. 마지막 규칙이 대장이 낡아 무의미해지는 것을 막는 유일한 장치다. 결함을 고친 뒤에는 :strict 가 초록이어야 하고 대장 줄을 지워야 한다.",
    "verify:fresh-schema": "node scripts/77-verify-fresh-schema.js",
    "verify:fresh-schema:strict": "cross-env VERIFY_STRICT=1 node scripts/77-verify-fresh-schema.js",
    "verify:schema": "npm run verify:fresh-schema && npm run test:schema-constraints",
    "_note:verify:schema": "★ 세션48 — 77 은 「빈 DB 가 운영과 같은가」를 정적으로, test_schema_constraints 는 「제약이 실제로 살아 있는가」를 실행으로 본다. 대체 관계가 아니다: EXPLAIN(GENERIC_PLAN) 은 NOT NULL·CHECK·FK 를 원리적으로 못 보고, src/ 가 값을 $1..$n 파라미터로 넘기므로 타입·길이·ENUM 검사도 걸리지 않는다. 세션48 강화로 77 의 뮤테이션 검출이 7/16 → 15/16 이 됐다(못 잡는 1종은 컬럼 순서 = 무해).",
    "migrate:all": "npm run migrate && psql $DATABASE_URL -v ON_ERROR_STOP=1 -f scripts/migrations/003_seed_products.sql",
    "_note:migrate:all": "⚠ 003_seed_products.sql 은 평범한 INSERT 다 — 멱등하지 않다. 빈 DB 에 딱 한 번만 쓸 것. 운영에는 절대 돌리지 말 것(229,028행이 이미 있다).",
    "migrate:020": "psql $DATABASE_URL -v ON_ERROR_STOP=1 -f scripts/migrations/020_allergen_evidence_level.sql",
    "_note:migrate:020": "020 은 이미 운영에 적용됐다(2026-07-31 확인). 000_baseline.sql 이 같은 내용을 포함하므로 단독 실행은 롤백 복구용으로만 남긴다."
  },
  "keywords": [
    "food-safety",
    "nutrition",
    "traffic-light",
    "korea"
  ],
  "author": "Jay Kim <saeronjhk@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "@capacitor/browser": "^8.0.3",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/share": "^8.0.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "express-async-errors": "^3.1.1",
    "express-rate-limit": "^8.3.2",
    "express-validator": "^7.3.2",
    "firebase-admin": "^12.7.0",
    "helmet": "^8.1.0",
    "morgan": "^1.10.1",
    "multer": "^2.1.1",
    "pg": "^8.20.0",
    "swagger-ui-express": "^5.0.1",
    "winston": "^3.19.0",
    "winston-daily-rotate-file": "^5.0.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.4",
    "cross-env": "^10.1.0"
  }
}
